node_modules
dist
out
.DS_Store
.eslintcache
*.log*

# Secrets — tokens for publishing / update downloads. NEVER commit.
.env
.env.local

# Code-signing certificates (private key). NEVER commit.
certs/
*.pfx

# Local Claude Code tooling config
.claude/
